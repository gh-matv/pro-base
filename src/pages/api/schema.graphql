
type Mission { 
  id: ID!
  status: String!
  startDate: String!
  endDate: String!
}

input MissionUpdate {
  id: ID!
  startDate: String
  endDate: String
}

# All the fields we can use to filter the missions
input MissionQuery {
  id: ID
  status: String
  missions_from: String
  missions_until: String
}

type Location {
  name: String!
  lat: Float!
  lng: Float!
}

type Query {

  missions(q:MissionQuery): [Mission!]!
  mission(id: ID!): Mission

  location(name: String!): Location
}

type Mutation {
  #createMission(description: String!, startDate: String!, endDate: String!): Mission
  updateMission(update: MissionUpdate!): Mission
  #deleteMission(id: ID!): Mission
}

